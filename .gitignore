# ── OS / Editor ───────────────────────────────────────────────
.DS_Store
*.zip
.vscode/
.idea/

# ── Environment / Secrets — NEVER commit these ────────────────
.env
.env.local
.env.*.local

# Google OAuth credentials and tokens
credentials.json
token.json
scripts/credentials.json
scripts/token.json
pipeline/credentials.json
pipeline/token.json

# ── Raw data — large files, not for version control ───────────
data/raw/
fcc_data_downloads/

# ── Python ────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# ── Claude Code local settings ────────────────────────────────
.claude/settings.local.json

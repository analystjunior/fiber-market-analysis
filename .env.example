# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# ── Supabase ──────────────────────────────────────────────────
# Project URL (safe to share — same as what's in data.js)
SUPABASE_URL=https://sveqgyhncdrjemohpwho.supabase.co

# Service role key — server/scripts ONLY, never put in browser code
SUPABASE_SERVICE_KEY=sb_secret_...

# ── FCC BDC API ───────────────────────────────────────────────
# Get token at: broadbandmap.fcc.gov → Account → Manage API Access
FCC_USERNAME=your_fcc_email@example.com
FCC_API_TOKEN=your_44_char_token_here
